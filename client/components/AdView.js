import React from 'react'
import PropTypes from 'prop-types'
import {connect} from 'react-redux'
import {Link} from 'react-router-dom'

const AdView = () => {
  return <div className="ad_View" />
}

export default AdView
